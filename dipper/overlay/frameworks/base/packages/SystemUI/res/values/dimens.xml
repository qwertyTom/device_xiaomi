<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="rounded_corner_content_padding">12dp</dimen>
    <dimen name="horizontal_max_swift">3.0dp</dimen>
    <dimen name="vertical_max_swift">1.0dp</dimen>
  
    <!-- the padding on the start of the statusbar -->
    <dimen name="status_bar_padding_start">12dp</dimen>

    <!-- the padding on the end of the statusbar -->
    <dimen name="status_bar_padding_end">4dp</dimen>

    <!-- Margin on the left side of the carrier text on Keyguard -->
    <dimen name="keyguard_carrier_text_margin">20dp</dimen>
 
    <!-- Margin on the right side of the system icon group on Keyguard. -->
    <dimen name="system_icons_keyguard_padding_end">8dp</dimen>
    
    <!-- The starting padding for the clock in the status bar. -->
    <dimen name="status_bar_clock_starting_padding">2.5dp</dimen>

    <!-- End padding for left-aligned status bar clock -->
    <dimen name="status_bar_left_clock_end_padding">2.5dp</dimen>

    <!-- Padding between signal cluster and battery icon -->
    <dimen name="signal_cluster_battery_padding">3dp</dimen>

    <dimen name="battery_level_padding_start">3dp</dimen>
</resources>
